import { GalleryImagesPipe } from './gallery-images.pipe';

describe('Pipe: GalleryImagesPipe', () => {
  let pipe: GalleryImagesPipe;
  beforeEach(() => {
    pipe = new GalleryImagesPipe();
  });
});
